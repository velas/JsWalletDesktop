// Generated by LiveScript 1.6.0
(function(){
  var localStorage, encrypt, aes, wordsToUtf8, seedEncrypt, name, del, setbkp, set, saved, get, out$ = typeof exports != 'undefined' && exports || this;
  localStorage = require('localStorage');
  encrypt = require('./pin.js').encrypt;
  aes = require('crypto-js/aes');
  wordsToUtf8 = require('./words-to-utf8.js');
  seedEncrypt = require('./seed-encrypt.js');
  name = 'sseed';
  out$.del = del = function(){
    return localStorage.setItem(name, "");
  };
  out$.setbkp = setbkp = function(){
    return localStorage.setItem('sseedbkp', localStorage.getItem(name));
  };
  out$.set = set = function(value){
    var key, res;
    key = encrypt(name);
    res = seedEncrypt.encrypt(value, key);
    return localStorage.setItem(name, res);
  };
  out$.saved = saved = function(){
    var ref$;
    return ((ref$ = localStorage.getItem(name)) != null ? ref$ : "") !== "";
  };
  out$.get = get = function(){
    var key, res, ref$;
    key = encrypt(name);
    res = (ref$ = localStorage.getItem(name)) != null ? ref$ : "";
    if (res === "") {
      return res;
    }
    return seedEncrypt.decrypt(res, key);
  };
}).call(this);
