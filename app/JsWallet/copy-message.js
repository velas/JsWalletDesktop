// Generated by LiveScript 1.6.0
(function(){
  var react, getPrimaryInfo, getLang;
  react = require('react');
  getPrimaryInfo = require('./get-primary-info.ls');
  getLang = require('./get-lang.ls');
  module.exports = function(store){
    var copied, copiedClass, style, lang, cut, copyStyle, children;
    copied = store.current.copied;
    if (copied === '') {
      return null;
    }
    copiedClass = copied === '' ? '' : 'opened';
    style = getPrimaryInfo(store);
    lang = getLang(store);
    cut = function(tx){
      var t, r;
      if (tx == null) {
        return 'none';
      }
      t = tx.toString();
      return r = t.substr(0, 10) + '..' + t.substr(tx.length - 25, 0) + '..' + t.substr(t.length - 10, 10);
    };
    copyStyle = {
      color: style.app.text,
      background: "rgb(255 255 255 / 10%)",
      backdropFilter: "blur(5px)",
      border: "1px solid " + style.app.border
    };
    return react.createElement('div', {
      key: "copy-message",
      style: copyStyle,
      className: copiedClass + " copied copied489183944"
    }, children = [
      react.createElement('div', {
        className: 'mb-5'
      }, ' ' + lang.copied), react.createElement('div', {
        className: 'contents'
      }, ' ' + cut(copied))
    ]);
  };
}).call(this);
