// Generated by LiveScript 1.6.0
(function(){
  var Bar, react, map, ethToVlx, getColor, buildDataset, buildData, legend;
  Bar = require('react-chartjs-2').Bar;
  react = require('react');
  map = require('prelude-ls').map;
  ethToVlx = require('../../web3t/addresses.js').ethToVlx;
  getColor = function(items){
    return function(item){
      var index;
      index = items.indexOf(item);
      return 3872625 + index * 2;
    };
  };
  buildDataset = function(items){
    return {
      data: map(function(it){
        return +it.reward;
      })(
      items),
      backgroundColor: map(getColor(items))(
      items)
    };
  };
  buildData = function(items){
    return {
      datasets: map(buildDataset)(
      items),
      labels: map(function(it){
        return it.epoch;
      })(
      items)
    };
  };
  legend = {
    display: false
  };
  module.exports = function(store, web3t){
    var data;
    if (store.staking.rewardInfo.length === 0) {
      return null;
    }
    data = buildData(store.staking.rewardInfo);
    return react.createElement(Bar, {
      data: data,
      width: 100,
      height: 100,
      legend: legend
    });
  };
}).call(this);
