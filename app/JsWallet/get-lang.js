// Generated by LiveScript 1.6.0
(function(){
  var ref$, objToPairs, pairsToObj, map, getLang;
  ref$ = require('prelude-ls'), objToPairs = ref$.objToPairs, pairsToObj = ref$.pairsToObj, map = ref$.map;
  getLang = function(index){
    return function(arg$){
      var name, value;
      name = arg$[0], value = arg$[1];
      return [name, value[index]];
    };
  };
  module.exports = function(store){
    var index;
    index = store.langs.languages.indexOf(store.lang);
    if (index === -1) {
      return null;
    }
    return pairsToObj(
    map(getLang(index))(
    objToPairs(
    store.langs.mapping)));
  };
}).call(this);
