// Generated by LiveScript 1.6.0
(function(){
  var react, getPrimaryInfo, round5, round, ref$, find, filter, times, keyboard, icons, getActualValues;
  react = require('react');
  getPrimaryInfo = require('../get-primary-info.ls');
  round5 = require('../round5.ls');
  round = require('../round.ls');
  ref$ = require('prelude-ls'), find = ref$.find, filter = ref$.filter;
  times = require('../math.ls').times;
  keyboard = require('./keyboard.ls');
  icons = require('../icons.ls');
  getActualValues = function(value){
    return function(item){
      if (value == null) {
        return false;
      }
      if (value === "") {
        return false;
      }
      return item.indexOf(value) === 0;
    };
  };
  module.exports = function(arg$){
    var store, value, onChange, placeholder, onKeyDown, list, style, baseStyle, inputStyle, ref$, btnIcon, actualPlaceholder, actualList, typeaheadPlaceholder, onTypeheadFocus, children;
    store = arg$.store, value = arg$.value, onChange = arg$.onChange, placeholder = arg$.placeholder, onKeyDown = arg$.onKeyDown, list = arg$.list;
    style = getPrimaryInfo(store);
    baseStyle = {
      background: style.app.wallet,
      color: style.app.text,
      overflowX: 'auto',
      marginBottom: '5px'
    };
    inputStyle = (ref$ = {}, import$(ref$, baseStyle), ref$.background = 'transparent', ref$);
    btnIcon = {
      filter: style.app.btnIcon
    };
    actualPlaceholder = placeholder != null ? placeholder : "";
    actualList = filter(getActualValues(value))(
    list);
    console.log({
      actualList: actualList,
      list: list
    });
    typeaheadPlaceholder = actualList[0];
    onTypeheadFocus = function(){
      return document.querySelector('.userinput').focus();
    };
    return react.createElement('div', {
      className: 'input-area input-area-1052438182'
    }, children = [
      react.createElement('input', {
        type: "text",
        style: inputStyle,
        placeholder: typeaheadPlaceholder,
        autoComplete: "off",
        onKeyDown: onKeyDown,
        onFocus: onTypeheadFocus,
        className: 'typeahead'
      }), react.createElement('input', {
        type: "text",
        value: value + "",
        style: inputStyle,
        onChange: onChange,
        placeholder: actualPlaceholder,
        autoComplete: "off",
        onKeyDown: onKeyDown,
        className: 'userinput'
      })
    ]);
  };
  function import$(obj, src){
    var own = {}.hasOwnProperty;
    for (var key in src) if (own.call(src, key)) obj[key] = src[key];
    return obj;
  }
}).call(this);
