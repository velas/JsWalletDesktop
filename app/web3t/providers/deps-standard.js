// Generated by LiveScript 1.6.0
(function(){
  var BitcoinLib, Web3, Tx, BN, hdkey, bip39, mnemonicToSeed, cache, solanaWeb3;
  BitcoinLib = require('bitcoinjs-lib');
  Web3 = require('web3');
  Tx = require('../node_modules_embed/ethereumjs-tx/index.js');
  BN = require('ethereumjs-util').BN;
  hdkey = require('../node_modules_embed/ethereumjs-wallet/hdkey.js');
  bip39 = require('bip39');
  //solanaWeb3 = require('../../web3_js');
  //tokenbridge = require('../../tokenbridge');
  mnemonicToSeed = bip39.mnemonicToSeed;
  cache = {};
  bip39.mnemonicToSeed = function(mnemonic){
    if (cache[mnemonic] == null) {
      cache[mnemonic] = mnemonicToSeed(mnemonic);
    }
    return cache[mnemonic];
  };
  module.exports = {
    BitcoinLib: BitcoinLib,
    Web3: Web3,
    Tx: Tx,
    BN: BN,
    hdkey: hdkey,
    bip39: bip39,
    //solanaWeb3: solanaWeb3,
    //ERC20BridgeToken: ERC20BridgeToken,
    //tokenbridge: tokenbridge
  };
}).call(this);
